#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - return integer
 *
 * @s: type char pointer
 *
 * Return: int value
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
