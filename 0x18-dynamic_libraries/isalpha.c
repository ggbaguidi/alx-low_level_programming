#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include "main.h"
#include "main.h"

/**
 * _isalpha - check if is alphanumeric
 * @c: a type int
 * Return: a type int
 */
int _isalpha(int c)
{
	return (isalpha(c));
}
