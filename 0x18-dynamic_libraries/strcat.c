#include "main.h"

/**
 * _strcat - concat two string
 * @dest: a char pointer
 * @src: a char pointer
 * Return: a char pointer
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
